@using Microsoft.AspNet.Identity

    <ul class="nav navbar-nav navbar-right">
        <li>@Html.ActionLink((Session["ScreenName"] == null? "Accedi" : "Esci"), (Session["ScreenName"] == null ? "Login" : "Logout"), "Users", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
    </ul>
